# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Downloaded data caches (nflverse pulls land here at build time), but keep the
# committed curated kicker / D-ST baseline.
data/*.csv
!data/kdst_baseline.csv

# Committed artifacts under outputs/ (projections.csv, meta.json) are small and
# checked in so the app runs immediately after cloning. Regenerate with
# scripts/build_projections.py. The live draft autosave is per-session, not committed.
outputs/draft_state.json

# OS / editor
.DS_Store
.ipynb_checkpoints/
.pytest_cache/
.streamlit/secrets.toml
